(function(){
	'use strict';
	angular.module('app', [
		// dependency injection
		
	])
	.config([
		// tool references
		// like logging libraries
		// and startup configurations
	])
		// Execute boostrapping code and any dependencies
	.run([
		'$q'
		,'$rootScope', function($q, $rootScope) {}
	])
})();